import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    container: {
        width: '100%',
        height: '100%',
        paddingLeft: '0px',
        paddingRight: 0,
    }
}));